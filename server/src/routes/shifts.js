import {Router} from 'express';import {Shift} from '../models/index.js';import {AppError,asyncHandler} from '../utils/http.js';const r=Router();r.get('/',asyncHandler(async(req,res)=>res.json(await Shift.find().sort('name'))));r.post('/',asyncHandler(async(req,res)=>res.status(201).json(await Shift.create(req.body))));r.put('/:id',asyncHandler(async(req,res)=>{const x=await Shift.findByIdAndUpdate(req.params.id,req.body,{new:true,runValidators:true});if(!x)throw new AppError(404,'Shift not found');res.json(x);}));r.delete('/:id',asyncHandler(async(req,res)=>{const x=await Shift.findByIdAndUpdate(req.params.id,{active:false},{new:true});if(!x)throw new AppError(404,'Shift not found');res.json(x);}));export default r;
