import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {FileSpreadsheet,CheckCircle2} from 'lucide-react';import {api,messageOf} from '../services/api';const fields=[['employeeId','Employee ID'],['employeeName','Employee name'],['date','Punch date'],['time','Punch time'],['type','Machine type']];export default function ImportAttendance(){const[file,setFile]=useState(),[inspection,setInspection]=useState(),[mapping,setMapping]=useState({}),[stage,setStage]=useState(''),[result,setResult]=useState(),[error,setError]=useState(''),nav=useNavigate();async function inspect(f){setFile(f);setError('');const fd=new FormData();fd.append('file',f);try{const{data}=await api.post('/attendance/inspect',fd);setInspection(data);setMapping(data.suggestedMapping)}catch(e){setError(messageOf(e))}}async function process(){setStage('Reading Excel…');const fd=new FormData();fd.append('file',file);fd.append('mapping',JSON.stringify(mapping));try{setTimeout(()=>setStage('Matching employees and calculating attendance…'),300);const{data}=await api.post('/attendance/import',fd);setResult(data);setStage('')}catch(e){setError(messageOf(e));setStage('')}}return <><div className="mb-6"><h1 className="page-title">Import attendance</h1><p className="muted mt-1">Upload the original biometric Excel export. The source data is never modified.</p></div><div className="card mx-auto max-w-3xl p-6">{!result?<>{error&&<div className="mb-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</div>}<label className="grid cursor-pointer place-items-center rounded-xl border-2 border-dashed border-slate-300 p-10 text-center hover:border-brand-500"><FileSpreadsheet className="mb-3 text-brand-600" size={36}/><div className="font-semibold">Choose Excel file</div><div className="mt-1 text-sm text-slate-500">.xls or .xlsx, up to 15 MB</div><input className="hidden" type="file" accept=".xls,.xlsx" onChange={e=>e.target.files[0]&&inspect(e.target.files[0])}/></label>{file&&<div className="mt-4 text-sm font-medium">Selected: {file.name}</div>}{inspection&&<div className="mt-6"><h2 className="font-bold">Confirm column mapping</h2><p className="muted mb-4 mt-1">{inspection.preview.length} preview rows · Sheet: {inspection.sheetNames[0]}</p><div className="grid gap-4 sm:grid-cols-2">{fields.map(([k,l])=><label key={k}><span className="label">{l}</span><select className="field" value={mapping[k]||''} onChange={e=>setMapping({...mapping,[k]:e.target.value})}><option value="">Not mapped</option>{inspection.headers.map(h=><option key={h}>{h}</option>)}</select></label>)}</div><button className="btn-primary mt-6" disabled={!!stage} onClick={process}>{stage||'Process attendance'}</button></div>}</>:<div className="text-center"><CheckCircle2 className="mx-auto text-emerald-600" size={52}/><h2 className="mt-4 text-2xl font-bold">Import complete</h2><div className="mx-auto mt-6 grid max-w-xl grid-cols-2 gap-3 text-left sm:grid-cols-4">{[['Raw records',result.rawRecords],['Matched employees',result.matchedEmployees],['Attendance created',result.attendanceCreated],['Exceptions',result.exceptions]].map(([k,v])=><div className="rounded-xl bg-slate-50 p-4" key={k}><div className="text-2xl font-bold">{v}</div><div className="text-xs text-slate-500">{k}</div></div>)}</div><div className="mt-7 flex justify-center gap-3"><button className="btn-secondary" onClick={()=>nav('/attendance/exceptions')}>View exceptions</button><button className="btn-primary" onClick={()=>nav('/attendance')}>View attendance</button></div></div>}</div></>}
