import {useEffect,useState} from 'react';import {Users,UserCheck,UserX,ClockAlert,TimerOff,ScanLine,TriangleAlert} from 'lucide-react';import {api} from '../services/api';import {Empty,fmtTime,Spinner,StatusBadge} from '../components/ui';const items=[['totalEmployees','Total Employees',Users,'bg-blue-50 text-blue-600'],['present','Present Today',UserCheck,'bg-emerald-50 text-emerald-600'],['absent','Absent Today',UserX,'bg-red-50 text-red-600'],['late','Late Today',ClockAlert,'bg-amber-50 text-amber-600'],['shortHours','Short Hours',TimerOff,'bg-orange-50 text-orange-600'],['missingPunches','Missing Punches',ScanLine,'bg-rose-50 text-rose-600'],['attendanceErrors','Errors',TriangleAlert,'bg-red-50 text-red-600']];export default function Dashboard(){const[d,setD]=useState();useEffect(()=>{api.get('/dashboard').then(r=>setD(r.data))},[]);if(!d)return <Spinner/>;return <><div className="mb-6"><h1 className="page-title">Attendance overview</h1><p className="muted mt-1">A clear view of today’s workforce activity.</p></div><div className="mb-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{items.map(([k,l,I,c])=><div className="card flex items-center gap-4 p-5" key={k}><div className={`rounded-xl p-3 ${c}`}><I/></div><div><div className="text-2xl font-bold text-ink">{d.stats[k]}</div><div className="text-sm text-slate-500">{l}</div></div></div>)}</div><div className="card"><div className="border-b p-5"><h2 className="font-bold text-ink">Recent attendance</h2></div>{d.recent.length?<div className="table-wrap"><table className="table"><thead><tr><th>Employee</th><th>ID</th><th>Clock In</th><th>Clock Out</th><th>Status</th></tr></thead><tbody>{d.recent.map(x=><tr key={x._id}><td className="font-medium">{x.employee?.name}</td><td>{x.employeeId}</td><td>{fmtTime(x.actualClockIn)}</td><td>{fmtTime(x.actualClockOut)}</td><td><StatusBadge value={x.conditions}/></td></tr>)}</tbody></table></div>:<Empty/>}</div></>}
