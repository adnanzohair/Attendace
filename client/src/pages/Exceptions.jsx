import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import {api} from '../services/api';import {Empty,StatusBadge} from '../components/ui';export default function Exceptions(){const[data,setData]=useState([]),[status,setStatus]=useState('open');const load=()=>api.get('/attendance/exceptions',{params:{status}}).then(r=>setData(r.data));useEffect(()=>{load()},[status]);async function resolve(x){await api.put(`/attendance/exceptions/${x._id}`,{status:'resolved',resolution:'Reviewed by HR'});load()}return <><div className="mb-6"><h1 className="page-title">Attendance exceptions</h1><p className="muted mt-1">Records that need a human decision.</p></div><div className="card"><div className="border-b p-4"><select className="field w-auto" value={status} onChange={e=>setStatus(e.target.value)}><option>open</option><option>resolved</option><option>ignored</option></select></div>{data.length?<div className="table-wrap"><table className="table"><thead><tr><th>Date</th><th>Employee</th><th>ID</th><th>Issue</th><th>Message</th><th>Action</th></tr></thead><tbody>{data.map(x=><tr key={x._id}><td>{x.workDate||'—'}</td><td>{x.employeeName||'Unknown'}</td><td>{x.employeeId||'—'}</td><td><StatusBadge value={x.type}/></td><td className="max-w-sm truncate">{x.message}</td><td>{x.attendance?<Link className="text-brand-600" to={`/attendance/${x.attendance._id||x.attendance}`}>Review</Link>:status==='open'?<button className="text-brand-600" onClick={()=>resolve(x)}>Resolve</button>:'—'}</td></tr>)}</tbody></table></div>:<Empty/>}</div></>}
