import {X} from 'lucide-react';export const Spinner=()=> <div className="mx-auto h-8 w-8 animate-spin rounded-full border-4 border-brand-100 border-t-brand-600"/>;export function StatusBadge({value}){const v=Array.isArray(value)?value[0]:value;const c=/Error|Absent|Missing/i.test(v)?'bg-red-50 text-red-700':/Late|Short/i.test(v)?'bg-amber-50 text-amber-700':/Present|Overtime|finalized/i.test(v)?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-600';return <span className={`badge ${c}`}>{v||'—'}</span>}export function Modal({title,children,onClose}){return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-4"><div className="card max-h-[92vh] w-full max-w-2xl overflow-auto p-6"><div className="mb-5 flex items-center justify-between"><h2 className="text-lg font-bold text-ink">{title}</h2><button onClick={onClose}><X/></button></div>{children}</div></div>}export const Empty=({text='No records found'})=><div className="p-10 text-center text-sm text-slate-500">{text}</div>;export const fmtTime=v=>v?new Date(v).toLocaleTimeString([],{hour:'numeric',minute:'2-digit'}):'—';export const hours=m=>`${Math.floor((m||0)/60)}h ${String((m||0)%60).padStart(2,'0')}m`;
